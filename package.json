{
  "name": "nextgen-medical-imaging",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud-native PACS - Medical Imaging Platform (Supabase + Vercel)",
  "scripts": {
    "install": "npm install && cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
