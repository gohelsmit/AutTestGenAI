# Supabase Edge Functions

Deploy with:

```bash
supabase functions deploy function-name
```

Or use the Supabase Dashboard → Edge Functions to create and deploy.

Example: an Edge Function can run after DICOM upload to trigger AI analysis or write to `audit_logs`.
